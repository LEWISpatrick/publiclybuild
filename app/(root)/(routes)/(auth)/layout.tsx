const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="w-full flex items-center justify-center mb-10">{children}</main>
  )
}

export default AuthLayout
